# Dev / test dependencies — install on top of requirements.txt:
#   .venv/bin/pip install -r requirements-dev.txt
# Versions pinned to what is installed in .venv (verified 2026-06-10).
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-cov==7.1.0
respx==0.23.1
freezegun==1.5.5
